import { Box } from '@mui/material';
import React from 'react';

type FooterProps = {};

const Footer = ({}: FooterProps) => {
  return <Box sx={{ width: '100%', height: '26.1875rem', background: 'black' }}></Box>;
};

export default Footer;
